package Question;

public final class questionFactory {
    /*
    public static question createQuestion(String question, String[] subject, String[] choices, String answer, String type) {
        return switch (type) {
            case "MultipleChoice" -> new questionMultipleChoice(question, subject, choices, answer);
            case "TrueFalse" -> new questionTrueFalse(question, subject, answer);
            case "ShortAnswer" -> new questionShortAnswer(question, subject, answer);
            default -> throw new IllegalStateException("Illegal Aug: " + type);
        };
    }
    */
}
